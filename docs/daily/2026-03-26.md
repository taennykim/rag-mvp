# Daily Log - 2026-03-26

## 1. 오늘 작업 내용
- `AGENTS.md`, `docs/status.md`, `docs/daily/2026-03-25.md`, `TODO.md`, `README.md`를 다시 확인했다.
- 오늘 우선 업무를 `indexing 상태 표시`, `retrieval 질문 세트 작성`, `retrieval 품질 점검` 중심으로 정리했다.
- `/upload` 페이지에서 업로드 파일별 `Indexed / Not indexed` 상태와 chunk 수를 표시하도록 수정했다.
- 업로드 목록이 `/upload/files`와 `/index/files`를 함께 조회해 `stored_name` 기준으로 상태를 계산하도록 정리했다.
- retrieval 점검용 문서별 질문 세트를 `docs/retrieval-test-set.md`로 정리했다.
- 기본 파일 3개 기준 retrieval 1차 pass/fail을 확인했다.
- 산출방법서 질문은 파일 단독 검색은 가능하지만 전체 파일 검색에서는 ranking 품질 이슈가 남는 것을 확인했다.
- retrieval 후보를 넓게 가져온 뒤 token overlap + 문자 n-gram overlap 기준으로 lexical rerank를 추가했다.
- rerank 후에도 산출방법서 질문 일부만 개선되고 핵심 병목은 여전히 embedding 품질임을 확인했다.
- `/upload` 화면에 `Primary parser`, `Auxiliary parser` 선택 UI를 추가했다.
- backend에 `GET /parse/parsers`를 추가하고 parsing/chunking/indexing 요청에 parser selection을 전달하도록 정리했다.
- parser 구조를 `Docling -> auxiliary parser fallback` 형태로 확장했다.
- 현재 환경에는 `Docling`이 설치되어 있지 않아 PDF는 `PyMuPDF`, DOCX는 `python-docx` fallback이 실제 사용되는 것을 확인했다.
- `DOC parser`, `Excel parser`는 선택 슬롯만 먼저 노출하고 미구현 안내가 나오도록 정리했다.
- frontend 헤더 문구를 `Uploaded Insurance documents`로 수정했다.
- 문서 정합성을 맞추기 위해 `README.md`, `docs/status.md`, `docs/backend.md`, `TODO.md`를 업데이트했다.

## 2. 완료된 작업
- Upload 목록 indexing 상태 표시 완료
- retrieval 질문 세트 작성 완료
- retrieval 1차 실행 및 pass/fail 기록 완료
- lexical rerank 보정 추가 및 재검증 완료
- parser selection UI 추가 완료
- parser catalog / fallback routing backend 반영 완료
- 문서 정합성 재정리 완료

## 3. 검증 내용
- frontend `npm run build` 검증 완료
- backend `py_compile` 검증 완료
- RAG 서버 backend `127.0.0.1:8000/health` 응답 확인 완료
- RAG 서버 frontend `127.0.0.1:3000/upload` 응답 확인 완료
- RAG 서버 frontend `127.0.0.1:3000/chat` 응답 확인 완료
- parser dropdown이 RAG 서버 `/upload` 화면에서 보이는 것까지 확인했다.

## 4. 이슈 및 메모
- 현재 실제 작업 기준 포트는 frontend `3000`, backend `8000`이다.
- `127.0.0.1:3001`은 현재 작업 중인 RAG 프런트가 아니므로 화면 확인 시 혼동이 생길 수 있다.
- `Docling`을 기본 파서로 UI에 노출했지만 아직 패키지가 설치되지 않아 fallback parser가 실제로 사용된다.
- `DOC parser`, `Excel parser`는 아직 구현 전이다.
- 산출방법서 retrieval 품질은 여전히 낮아 embedding 교체가 가장 큰 다음 과제다.
- GitHub push는 인증 문제로 아직 반영하지 못했다.

## 5. 다음 작업 계획
- `Docling` 설치 여부를 결정하고 설치 시 실제 primary parser 동작을 재검증한다.
- OpenAI 기반 실제 embedding 모델로 교체할 환경 준비 항목을 정리한다.
- 기존 hash embedding 기반 Chroma 데이터를 재인덱싱할 절차를 정리한다.
- retrieval 질문 세트 기준으로 embedding 교체 전후 결과를 비교한다.
- retrieval 안정화 후 answer generation 연결에 들어간다.

## 6. 다음 세션 시작 포인트
1. `docs/status.md` 확인
2. `docs/retrieval-test-set.md` 확인
3. 현재 parser 상태와 `Docling` 설치 필요 여부 확인
4. embedding 교체 또는 parser 고도화 중 우선순위 선택
