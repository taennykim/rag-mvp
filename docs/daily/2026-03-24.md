# Daily Log - 2026-03-24

## 1. 오늘 작업 내용
- 원격 저장소 최신 `AGENTS.md`, `TODO.md`를 다시 pull해서 기준 문서를 재확인했다.
- 현재 기준 문서에 맞춰 필요한 `docs/` 구조를 생성했다.
- `plan.md`, `retrieval.md`, `llm.md`, `ui.md`를 작성했다.
- 기준 EC2 `i-0279082dd48ce62d2`의 실제 스펙을 조회했다.
- 동일 스펙으로 구현 서버를 만들기 위한 Terraform 초안을 추가했다.
- 루트 EBS와 태그 구성을 추가 조회해 Terraform 값을 기준 인스턴스에 맞게 보정했다.
- 현재 문서와 실제 파일 상태 차이를 반영해 상태 문서를 정리했다.
- Terraform 기본 instance name을 `c1an2testadmin001_RAC`로 수정했다.
- 권한 상승 환경에서 `terraform plan`을 재검증했다.
- `terraform apply`로 EC2를 생성하고 상태를 확인했다.
- `frontend/`에 Next.js App Router 기준 최소 골격을 추가했다.
- `backend/`에 FastAPI 기준 최소 API 골격을 추가했다.
- backend Python 소스 문법을 확인했고 frontend `package.json` 형식을 검증했다.
- 구현 서버 인스턴스 이름을 `c1an2testadmin001_RAG`로 정정했다.
- 현재 서버와 RAG 서버 사이 `2022/tcp`를 CIDR이 아니라 security group reference 기준으로 열었다.
- 현재 서버에서 RAG 서버로 `ssh -p 2022` 연결을 검증했다.
- RAG 서버에 현재 프로젝트를 `rsync`로 동기화했다.
- RAG 서버에 `Node.js 20`, `npm`, `pip3`, `python3.10-venv`를 설치했다.
- RAG 서버에서 frontend/backend 의존성 설치를 완료했다.
- backend에 PDF/DOCX 업로드 API와 업로드 파일 목록 API를 구현했다.
- backend에 기본 업로드 파일 디렉토리와 기본 파일 업로드 API를 추가했다.
- 업로드 파일 저장 경로를 `backend/data/uploads`로 연결했다.
- frontend `/upload` 페이지에 실제 파일 업로드 UI와 파일 목록 UI를 추가했다.
- frontend `/upload` 페이지에서 `Default file` 버튼으로 미리 저장한 파일을 선택 업로드할 수 있게 했다.
- RAG 서버에서 backend `uvicorn` 실행 후 upload endpoint를 실제 호출해 업로드와 목록 조회를 검증했다.

## 2. 완료된 작업
- `docs/` 폴더 생성
- `docs/daily/` 폴더 생성
- `docs/plan.md` 생성
- `docs/retrieval.md` 생성
- `docs/llm.md` 생성
- `docs/ui.md` 생성
- `docs/aws.md` 생성
- `docs/status.md` 생성
- `infra/terraform/` 생성
- Terraform plan 재검증 완료
- EC2 `c1an2testadmin001_RAG` 상태 반영 완료
- `frontend/` skeleton 생성
- `backend/` skeleton 생성
- RAG 서버 개발 환경 준비 완료
- Upload Phase 최소 구현 완료
- 오늘 daily 기록 생성

## 3. 이슈 및 문제
- Terraform AWS provider는 샌드박스 환경에서는 정상 동작하지 않았고, 권한 상승 환경에서만 plan/apply가 가능했다.
- GitHub private repo 직접 clone 인증이 안 돼서 현재는 `rsync`로 코드를 동기화했다.
- frontend `next@15.2.4`는 보안 경고가 있어 추후 패치 버전으로 올릴 필요가 있다.
- frontend 실행 검증은 아직 남아 있다.

## 4. 다음 작업 계획
- frontend/backend 실행 검증
- parsing API 실제 구현
- PDF/DOCX 텍스트 추출과 빈 텍스트 검증 추가
- upload 이후 parsing 흐름 연결

## 5. 내일 다시 시작할 때
- 먼저 `docs/status.md`를 열어 최신 완료 범위와 남은 작업을 확인한다.
- 다음으로 `docs/daily/2026-03-24.md`를 열어 오늘 작업 맥락을 이어받는다.
- 그 다음 `TODO.md` 기준으로 frontend 실행 검증부터 진행한다.
- 첫 구현 작업은 parsing API와 parsing 테스트 응답 구현으로 잡는다.
