# Daily Log - 2026-04-14

## 1. 오늘 작업 내용
- 작업 시작 전에 `AGENTS.md`, `README.md`, `docs/plan.md`, `TODO.md`, `docs/status.md`, 관련 `docs/*.md`, 최신 daily 흐름을 다시 확인했다.
- `docs/chat_plan.md`, `docs/answer-eval.md`, `docs/retrieval-test-set.md`를 기준으로 현재 `/chat` 다음 우선 작업이 Step 4 이후 평가/분기 정리와 answer/citation 품질 기록 보강임을 재확인했다.
- 최근 세션 흐름상 backend `/chat`은 `Step 1`, `Step 2`, `Step 3`, `Step 5`까지 반영되었고, 다음 작업은 `Step 4. Search API 호출` 단계 분리와 `Step 6. Search Result Evaluation` 규칙 정리로 이어져야 함을 확인했다.
- parser 운영 정책은 현재 `Legacy auto` 기본, `Docling` 비교 검증용, `Docling(md)` Markdown 산출물 생성용으로 유지되고 있음을 다시 확인했다.
- RAG 서버 화면 테스트는 서버 기준 `127.0.0.1:3000/8000`에서만 수행하고, 현재 서버에서는 CLI 작업과 문서 업데이트만 진행해야 한다는 운영 규칙을 다시 확인했다.
- backend `/chat`에서 Search API 호출 계층을 `execute_search_for_chat`으로 분리했다.
- backend `/chat`에 `retrieved_chunks` 기준 rule-based Search Result Evaluation을 추가하고 `need_more_context`, `search_evaluation`, `search_query`, `executed_search_queries`를 응답에 포함하도록 반영했다.
- frontend `/chat`에서 rewrite trace 아래에 search/evaluation trace를 함께 확인할 수 있도록 정리했다.
- query rewrite 운영 기준을 `docs/query-rewrite-spec.md`로 분리하고, backend prompt가 해당 문서를 읽어 `rewritten_query` 생성 기준에 반영하도록 수정했다.
- `/chat` main UI는 `LLM Question`에 `rewritten_query`만 보이도록 다시 단순화했고, 내부 Search 후보와 rerank 기준도 `rewritten_query` 우선으로 정리했다.
- `/chat` Answer 카드 상단에 프런트 기준 요청-응답 시간(`Response time`)을 표시하도록 추가했다.
- 마지막 고객 발화가 군더더기 표현 위주일 때 `rewritten_query`가 해당 문장만 거의 복사하는 문제를 보완하기 위해, query rewrite seed를 최근 고객 발화 묶음 기준으로 보강하고 filler 제거 규칙을 추가했다.
- 오늘 작업 내용을 `docs/plan.md`, `TODO.md`, `docs/status.md`, `docs/backend.md`, `docs/ui.md`, `docs/daily/2026-04-14.md`에 반영했다.

## 2. 완료된 작업
- 오늘 세션 시작 전 필수 문서 검토 완료
- 최근 daily 흐름과 현재 우선순위 재정리 완료
- 오늘 작업 시작점과 문서 기준선 확인 완료
- `Step 4. Search API 호출` backend 계층 분리 완료
- `Step 6. Search Result Evaluation` rule-based 1차 구현 완료
- `/chat` search/evaluation trace UI 반영 완료
- query rewrite spec 문서 분리 및 backend 연결 완료
- `/chat` rewritten_query-only UI 및 rewritten_query 우선 Search 정리 완료
- `/chat` Response time 표시 추가 완료
- query rewrite seed/filler 보정 완료

## 3. 이슈 및 문제
- `need_more_context`는 현재 평가 trace만 반영되어 있고 Lookup API 실제 호출 분기와는 아직 연결되지 않았다.
- answer/citation 품질 기록은 문서 템플릿이 준비되어 있으나 실제 누적 사례는 아직 충분하지 않다.
- 산출방법서 계열 retrieval 품질 이슈와 PDF garbled detection false negative 보정은 여전히 후속 점검이 필요하다.

## 4. 다음 작업 계획
- `docs/chat_plan.md` 기준 `Step 7. Need More Context` 분기와 `Step 8. Lookup API 호출` 연결 방식을 정리한다.
- `docs/answer-eval.md` 템플릿으로 대표 질문의 answer/citation 품질 기록을 이어간다.
- `docs/query-rewrite-spec.md`를 계속 보강해 query rewrite 해석 규칙과 예시를 정리한다.
