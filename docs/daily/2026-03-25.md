# Daily Log - 2026-03-25

## 1. 오늘 작업 내용
- `/upload` 페이지 UI를 정리했다.
- uploaded file list 초기화 버튼을 추가했다.
- uploaded file list에 파일명, 용량, 업로드 시간을 표시하도록 수정했다.
- default file 전용 버튼을 제거하고 `Upload file` 버튼 하나로 업로드 흐름을 통합했다.
- default file 선택 후 업로드 시 잘못된 메시지가 나오던 문제를 수정했다.
- backend 업로드 목록 응답에 `uploaded_at` 필드를 추가했다.
- backend `DELETE /upload/files` API를 추가했다.
- backend parsing 기능을 구현했다.
- PDF는 `PyMuPDF`, DOCX는 `python-docx`로 텍스트를 추출하도록 연결했다.
- 추출 텍스트가 비어 있는 경우 오류를 반환하도록 검증을 추가했다.
- `GET /parse`, `POST /parse`, `GET /parse/{stored_name}` API를 추가했다.
- RAG 서버에서 PDF/DOCX 추출 결과를 실제로 확인했다.
- RAG 서버에서 default file 업로드 후 parsing API 호출까지 실제로 검증했다.
- `/upload` 페이지에 파일별 `Parse test` 버튼을 추가했다.
- parsing test result 카드에서 preview, full extracted text, raw JSON을 확인할 수 있게 했다.
- DOCX 파서가 표 내용을 놓치던 문제를 확인하고 table, header/footer 추출까지 포함하도록 보강했다.
- parsing 결과와 reference extractor 결과를 비교하는 품질 지표를 추가했다.
- `Jaccard Similarity`와 `Levenshtein Distance`를 parse 응답과 UI에 표시했다.
- `Jaccard Similarity < 0.8`인 경우 `파싱 품질주의`를 빨간색으로 표시하도록 추가했다.
- RAG 서버에서 DOCX sample parsing length가 `1087`에서 `22887`로 증가한 것을 확인했다.
- RAG 서버에서 DOCX sample의 `Jaccard Similarity`가 약 `0.994`로 계산되는 것을 확인했다.
- parse 속도 개선을 위해 parsing 결과와 품질 점수 계산을 분리했다.
- `Parse test`는 텍스트 추출 결과만 보여주도록 유지하고, `Parsing test result` 카드에 `Check parsing quality` 버튼을 추가했다.
- backend에 `POST /parse/quality`를 추가하고 품질 점수 계산을 이 endpoint로 분리했다.
- chunking 단계를 구현했다.
- backend에 `GET /chunk`, `POST /chunk`, `GET /chunk/{stored_name}`를 추가했다.
- parsing 텍스트를 target length `800`, overlap `120` 기준으로 chunk로 나누도록 구현했다.
- chunk metadata에 `chunk_index`, `text`, `text_length`, `start_char`, `end_char`, `preview`를 포함했다.
- 파일별 chunk count를 `backend/data/chunk-metadata/{stored_name}.json`에 저장하도록 구현했다.
- RAG 서버에서 DOCX sample 기준 `38`개 chunk가 생성되는 것을 검증했다.
- chunk metadata 문서에 `source`, `page_number`, `section_header`와 각 필드 설명을 추가했다.
- backend에 `GET /index`, `POST /index`, `GET /index/{stored_name}`를 추가했다.
- chunk -> embedding -> Chroma 저장까지 한 번에 수행되는 최소 indexing 흐름을 구현했다.
- 로컬 해시 기반 최소 embedding을 사용해 외부 API 키 없이 indexing 검증이 가능하도록 했다.
- DOCX default file 기준 `38`개 chunk가 Chroma에 저장되는 것을 실제로 검증했다.
- backend에 `GET /index/files`, `DELETE /index/files`를 추가해 indexed 파일 목록 조회와 Chroma index 초기화를 지원하도록 했다.
- backend에 `GET /retrieve`, `POST /retrieve`를 추가해 query 기반 retrieval를 수행하도록 했다.
- retrieval 응답에 `source`, `chunk_index`, `preview`, `page_number`, `section_header`, `distance`를 포함했다.
- `/chat` 화면에 retrieval 테스트 UI를 추가했다.
- `/chat`의 `Target file`은 업로드 파일 전체가 아니라 실제 index된 파일만 보이도록 변경했다.
- `/chat`에 `Refresh indexed files`, `Reset indexed files` 버튼을 추가했다.
- `/upload` 화면에서 파일 업로드 후 자동으로 indexing까지 수행되도록 수정했다.
- `/upload` 업로드 완료 메시지에 저장된 chunk 수를 표시하도록 수정했다.
- `/upload` 목록에 `Delete upload + index` 버튼을 추가해 파일 1건 삭제 시 Chroma index와 chunk metadata도 함께 삭제되도록 했다.
- `/upload`의 `Default file list` 기본값을 `Select a file`로 변경했다.
- `/upload`의 `Clear list` 버튼은 제거했다.
- frontend 실행 환경을 정리하고 `3000` 포트, backend `8000` 포트 기준으로 다시 확인했다.
- parsing UI는 별도 `/parse` 화면 추가 시도 후 원복하고, 다시 `/upload` 화면 중심 구조로 유지했다.
- GitHub push까지 완료했다.

## 2. 완료된 작업
- upload UI 수정 완료
- uploaded file list 초기화 기능 완료
- 업로드 목록 메타데이터 표시 개선 완료
- default file 업로드 오류 수정 완료
- parsing 최소 기능 구현 완료
- parsing API 실제 검증 완료
- parsing 검증 UI 추가 완료
- DOCX parser 보강 완료
- parsing 품질 지표 추가 완료
- parse/quality 분리 완료
- chunking 최소 기능 구현 완료
- chunk metadata 문서 보강 완료
- indexing API 최소 구현 완료
- Chroma 저장 검증 완료
- retrieval API 최소 구현 완료
- chat retrieval UI 추가 완료
- 업로드 후 자동 indexing 연결 완료
- 파일 단위 upload + index 삭제 완료
- frontend 실행 포트 정리 완료

## 3. 이슈 및 문제
- `.doc`, `.xlsx` 파일은 기본 파일 폴더에 존재하지만 현재 지원 범위는 아니다.
- quality score는 자동 비교 지표이므로 최종 정확도 판단에는 원문 대조가 추가로 필요하다.
- chromadb telemetry 관련 warning이 출력되지만 indexing 동작 자체는 완료된다.
- frontend는 `localhost:3000`, backend는 `localhost:8000` 고정 포트 기준으로 확인했다.
- Next dev 서버는 `.next` 캐시 꼬임으로 `Cannot find module './xxx.js'` 오류가 반복돼, 현재는 `build + start` 방식이 더 안정적이다.
- 같은 원본 파일을 여러 번 업로드하면 `stored_name`이 계속 달라져 indexed 파일도 중복으로 누적된다.

## 4. 다음 작업 계획
- Upload 목록에서 파일별 indexing 상태를 직접 표시하기
- retrieval 품질을 질문 세트 기준으로 점검하고 `top_k`와 metadata 노출 방식을 다듬기
- `/chat`의 `Retrieval test` 정확도를 높이기 위해 query 예시별 결과를 비교하고 chunk 전략 또는 embedding 방식을 조정하기
- hash 기반 embedding을 실제 embedding 모델로 교체할 방식 정하기

## 5. 내일 다시 시작할 때
- 먼저 `docs/status.md`를 열어 retrieval까지 반영된 현재 상태와 다음 우선순위를 확인한다.
- 그 다음 `docs/backend.md`, `docs/chunking.md`, `TODO.md`를 보고 실제 embedding 교체 범위를 맞춘다.
- 첫 구현 작업은 `/chat`의 `Retrieval test` 정확도 점검 또는 Upload 목록의 indexing 상태 표시 중 하나로 잡는다.
