# Daily Log - 2026-04-16

## 1. 오늘 작업 내용
- 작업 시작 전에 `AGENTS.md`, `README.md`, `docs/plan.md`, `TODO.md`, `docs/status.md`, 관련 `docs/*.md`, 최신 daily 흐름을 다시 확인했다.
- Query Rewrite LLM 선택지 운영을 정리했다.
- Query Rewrite LLM 기본 backend 모델을 `gpt-4o-mini`로 두고, UI 기본 선택값은 공백 `Default`로 유지하도록 정리했다.
- RAG 서버에서 `gpt-4.1-mini` Azure OpenAI deployment 직접 호출이 성공하는 것을 확인했다.
- `gpt-4.1-mini`를 Query Rewrite LLM 선택지에 추가했다.
- `/chat` Search API endpoint 기본값을 `http://10.160.98.123:8000/api/search`로 바꾸고, Lookup API endpoint 입력을 `Lookup API endpoint(Later)` 라벨과 disabled 상태로 정리했다.
- Search API 연결 실패 시에도 `LLM Question`이 보이도록 backend `/chat` 오류 경로를 보강했다.
- frontend는 에러 응답에 `rewritten_query`가 있으면 결과를 유지하도록 보강했다.
- `/chat` Search API endpoint와 Lookup API endpoint 입력을 제거하고 backend 고정 endpoint를 사용하도록 바꿨다.
- `/chat`에 Search `final_k` 입력을 추가했다.
- `/chat`에 `Get response`와 `Get lookup response` 버튼을 분리하고, Search action은 Search API만, lookup action은 Lookup API만 호출하도록 연결했다.
- Lookup action은 직전 Search 결과 중 최고 `rrf_score` hit의 `document_id`를 사용하도록 조정했다.
- RAG 서버에 backend/frontend를 동기화하고 재시작했다.
- RAG 서버 `/chat`에서 잘못된 Search API endpoint(`http://127.0.0.1:1/api/search`)로 테스트했을 때도 `rewritten_query`와 오류 메시지가 함께 반환되는 것을 확인했다.

## 2. 완료된 작업
- `gpt-4.1-mini` Query Rewrite LLM 추가 및 호출 검증 완료
- Search API endpoint / Lookup API endpoint UI 정리 완료
- Search API 실패 시 `LLM Question` 유지 동작 보강 완료
- Search `final_k` 입력 및 Search/Lookup 단독 호출 UI/backend 연결 완료
- RAG 서버 반영 및 `/chat` API 검증 완료

## 3. 이슈 및 문제
- Search API 연결 timeout이 긴 경우 프론트 요청 완료까지 시간이 오래 걸릴 수 있다.
- 현재 프론트는 backend가 200 응답으로 rewrite 결과를 돌려줄 때 `LLM Question`을 유지하므로, 검색 실패 UX는 개선됐지만 timeout 자체를 줄이려면 backend external search timeout 조정이 추가로 필요할 수 있다.
- 치조골 이식/수술특약/판결 케이스 rewrite 보강은 아직 남아 있다.
- Lookup API는 `document_id`가 필요하므로, 직전 Search 결과가 없는 상태에서 Lookup을 누른 경우 UX를 추가 점검할 필요가 있다.

## 4. 다음 작업 계획
- 치조골 이식/수술특약/판결 케이스 query rewrite 규칙을 보강한다.
- Search API timeout 및 실패 메시지 UX를 더 정리할지 검토한다.
- RAG 서버 브라우저 화면에서 `LLM Question`, Query Rewrite LLM 선택 UI, Search/Lookup 버튼 동작, 단계별 응답시간 표시를 다시 점검한다.
