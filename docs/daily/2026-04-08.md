# Daily Log - 2026-04-08

## 1. 오늘 작업 내용
- 작업 시작 전에 `AGENTS.md`, `README.md`, `docs/plan.md`, `TODO.md`, `docs/status.md`, 관련 `docs/*.md`, 최신 daily 흐름을 다시 확인했다.
- 현재 워크스페이스 기준 진행 중인 저장소가 `rag-mvp`인지 다시 확인했다.
- RAG 서버 접속에 사용할 PEM 키가 `p2an2test001.pem`임을 확인했다.
- RAG 서버에 접속해 `rag-mvp` runtime 상태와 `run-logs/frontend.out`, `run-logs/backend.out`를 확인했다.
- frontend는 `next start`가 떠 있었지만 `.next` production build를 찾지 못해 실제로는 정상 기동하지 못한 상태임을 확인했다.
- backend는 이전 `uvicorn` 프로세스가 내려간 상태여서 둘 다 재기동이 필요했다.
- RAG 서버에서 frontend를 `npm run build` 후 `npm run start -- --hostname 127.0.0.1 --port 3000`으로 재기동했다.
- RAG 서버에서 backend를 `.venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000`으로 재기동했다.
- 재기동 후 `http://127.0.0.1:3000/upload` 응답 `200`, `http://127.0.0.1:8000/health` 응답 `200`을 확인했다.
- 오늘 작업 내용을 `README.md`, `docs/plan.md`, `TODO.md`, `docs/status.md`, `docs/daily/2026-04-08.md`에 반영했다.

## 2. 완료된 작업
- `rag-mvp` 단일 저장소 기준 작업 범위 재확인 완료
- RAG 서버 접속 키 확인 완료
- frontend build 누락 상태 확인 완료
- frontend `build + start` 재기동 완료
- backend `uvicorn` 재기동 완료
- `3000/8000` 헬스체크 재확인 완료
- 관련 md 문서 업데이트 완료

## 3. 이슈 및 문제
- frontend는 `next start`만 실행되어 있어도 `.next` production build가 없으면 실제 서비스가 뜨지 않는다.
- 따라서 UI 확인 전에는 `run-logs/frontend.out`에서 build 누락 에러를 먼저 확인하는 편이 안전하다.
- backend/frontend 재기동 시 포트 점유 프로세스를 먼저 정리하지 않으면 stale runtime과 로그가 섞일 수 있다.
- 현재 미추적 문서 `docs/external-retrieval-api.md`는 오늘 작업 범위에 포함하지 않았다.

## 4. 다음 작업 계획
- RAG 서버 UI 확인 전 `3000/8000` 응답과 `run-logs/*.out` 상태를 먼저 점검한다.
- 외부 RAG contract 확정 전 기준 `/chat` answer/citation shell 화면을 다시 확인한다.
- `docs/answer-eval.md` 기준으로 retrieval 질문 세트의 answer/citation 품질 기록을 이어간다.
- 이후 parser false negative 보정과 parser 영향 기반 retrieval 재검증을 진행한다.
