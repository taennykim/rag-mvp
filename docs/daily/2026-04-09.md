# Daily Log - 2026-04-09

## 1. 오늘 작업 내용
- 작업 시작 전에 `AGENTS.md`, `README.md`, `docs/plan.md`, `TODO.md`, `docs/status.md`, 관련 `docs/*.md`, 최신 daily 흐름을 다시 확인했다.
- GitHub SSH 연결을 정리해 `git fetch origin`이 가능하도록 설정했다.
- GitHub와 현재 로컬 차이를 비교해 원격 `origin/main`에 `docs/chat_plan.md` 문서 커밋 1건이 추가된 상태임을 확인했다.
- RAG 서버 frontend/backend를 다시 재기동하고 `127.0.0.1:3000/upload`, `127.0.0.1:8000/health` 응답 `200`을 확인했다.
- `/chat` 질문 `보험 계약 변경은?` 기준으로 실제 retrieval hit, `rerank_score`, `matched_queries`, `distance`를 추적했다.
- `/chat` UI에서 `Evidence`와 `Reference context`가 같은 preview를 중복 표시하던 구조를 정리했다.
- backend `citations`는 compact citation pointer 중심으로 축소하고 rank를 함께 내려주도록 수정했다.
- frontend `Reference context`에는 `Context #`, `distance`, `rerank_score`, `matched_queries`를 직접 표시하도록 수정했다.
- local frontend `npm run build`와 backend `py_compile`로 변경 사항을 검증했다.
- 오늘 작업 내용을 `README.md`, `docs/plan.md`, `docs/status.md`, `docs/ui.md`, `TODO.md`, `docs/daily/2026-04-09.md`에 반영했다.

## 2. 완료된 작업
- GitHub SSH 연결 정상화 완료
- GitHub 원격 차이 확인 완료
- RAG 서버 frontend/backend 재기동 및 `3000/8000` 응답 확인 완료
- `/chat` retrieval trace 확인 완료
- `Evidence` / `Reference context` 역할 분리 완료
- `/chat` `rerank_score` / `matched_queries` 표시 추가 완료
- local frontend/backend 검증 완료

## 3. 이슈 및 문제
- `rerank_score`, `matched_queries`는 현재 internal retrieval 경로에서만 안정적으로 내려오므로 외부 RAG endpoint 응답과는 별도 매핑이 필요하다.
- 현재 `Evidence`와 `Reference context`는 역할은 분리됐지만 여전히 같은 retrieval hit 집합을 기반으로 한다.
- answer 품질 자체와 citation 적절성 평가는 아직 질문 세트 기준으로 충분히 기록되지 않았다.

## 4. 다음 작업 계획
- RAG 서버 화면에서 변경된 `/chat` UI를 실제로 다시 확인한다.
- `docs/answer-eval.md` 기준으로 retrieval 질문 세트의 answer/citation 품질 기록을 이어간다.
- 이후 garbled detection false negative 보정과 parser 영향 기반 retrieval 재검증을 진행한다.
