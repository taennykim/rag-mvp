# Daily Log - 2026-03-31

## 1. 오늘 작업 내용
- 작업 시작 전 `AGENTS.md`, `TODO.md`, `docs/plan.md`, `docs/*.md`, `docs/daily/2026-03-30.md`를 다시 읽고 어제 수정된 md 파일을 리뷰했다.
- `README.md`, `TODO.md`, `docs/plan.md`, `docs/status.md`, `docs/llm.md`, `docs/daily/2026-03-30.md`, `docs/answer-eval.md`를 교차 확인해 문서 간 현재 상태와 재시작 순서 불일치를 점검했다.
- `TODO.md`의 next session 기준 날짜가 `2026-03-29`로 남아 있던 부분을 오늘 기준 흐름에 맞게 정리했다.
- `docs/plan.md`의 현재 단계와 다음 우선 작업을 answer 실질문 검증 완료 이후 상태에 맞게 갱신했다.
- `docs/status.md`와 `README.md`의 재시작 순서를 `docs/daily/2026-03-30.md` -> `docs/status.md` -> `docs/answer-eval.md` 기준으로 맞췄다.
- `README.md`의 embedding 상태 설명을 현재 사실에 맞게 수정하고, 오늘 품질최적화 작업이 embedding 재교체가 아니라 answer/parser/chunk 품질 재검증임을 분명히 했다.
- 재시작 문서 순서를 `AGENTS.md` -> `docs/plan.md` -> `TODO.md` -> `docs/status.md` -> 관련 `docs/*.md` -> 최신 `docs/daily/*`로, 마무리 업데이트 순서를 `docs/plan.md` -> `TODO.md` -> `docs/status.md` -> 필요한 `docs/*.md` -> `docs/daily/YYYY-MM-DD.md`로 정리했다.
- upload 고도화 방향을 다시 정리하고, `Uploaded file list` 중심으로 preview/quality를 보도록 frontend와 backend 메타데이터를 수정했다.
- backend parse summary에 preview와 quality 메타데이터를 반영하고, `GET /pipeline/files`에서 파일별 preview/quality 상태를 내려주도록 정리했다.
- frontend `/upload`에서 파일 행 기준 `Preview`, `Check quality`, `Reset` 액션을 정리하고, 하단 `Parsing test result`는 preview-only 형태로 단순화했다.
- local 기준 backend `py_compile`, frontend `npm run build` 검증을 다시 완료했다.

## 2. 완료된 작업
- 어제 수정된 주요 md 파일 리뷰 완료
- 문서 간 재시작 순서 정렬 완료
- embedding 현재 상태 설명 정정 완료
- quality optimization 시작 기준 문서 정리 완료
- 재시작/마무리 문서 workflow 정리 완료
- upload list preview / quality UI 정리 완료
- upload backend preview / quality metadata 반영 완료
- 오늘 daily 기록 생성

## 3. 이슈 및 문제
- `docs/answer-eval.md`는 오늘 작업 기준 핵심 문서지만 현재 git 추적 상태는 별도 확인이 필요하다.
- 문서 기반 개발 흐름에서는 restart guide가 여러 문서에 중복되므로, 이후에도 같은 내용이 분기되지 않게 주기적으로 맞춰야 한다.
- upload 고도화는 local build 기준 검증했지만, 실제 RAG 서버 `/upload` 화면 기준 최종 확인은 아직 남아 있다.

## 4. 다음 작업 계획
- `docs/answer-eval.md` 템플릿 기준으로 retrieval 질문 세트 일부를 실제로 채운다.
- retrieval fail, answer fail, citation fail을 분리 기록한다.
- RAG 서버 `/upload` 화면에서 preview / quality / failure history 표시를 실제로 검증한다.
- 산출방법서 계열 문서의 chunk 전략 보정 포인트를 별도 문서에 정리한다.
