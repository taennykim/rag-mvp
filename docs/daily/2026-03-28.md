# Daily Log - 2026-03-28

## 1. 오늘 작업 내용
- RAG 서버 기준 현재 작업 우선순위를 다시 확인하기 위해 `README.md`, `docs/status.md`, `TODO.md`를 검토했다.
- 오늘 기준 남은 핵심 작업을 `PDF parser 품질 비교`, `parser 영향 기반 retrieval 재검증`, `embedding 교체 준비`로 다시 정리했다.
- RAG 서버 frontend/backend 실행 상태를 점검하고 실제 프로세스 및 포트 응답을 확인했다.
- RAG 서버 backend를 다시 기동하고 `127.0.0.1:8000/health` 응답을 재확인했다.
- RAG 서버 frontend 기존 점유 프로세스를 정리한 뒤 다시 기동하고 `127.0.0.1:3000/upload`, `/chat` 응답을 재확인했다.
- 화면이 바로 보이지 않는 원인이 `127.0.0.1` 바인딩과 외부 브라우저 접근 차이 때문임을 확인했다.
- port forwarding 기준 접속 흐름을 정리하고, 이후 화면 요청 시 포워딩 명령과 브라우저 주소를 함께 안내하는 운영 원칙을 맞췄다.
- parser 옵션 노출 유지 여부를 검토했고, 현재 MVP 단계에서는 옵션을 넓히기보다 기본 parser 정책을 우선 정리하는 쪽이 더 적절하다고 판단했다.
- `TODO.md`를 현재 실제 완료 범위 기준으로 정리했다.
- `README.md`와 `docs/status.md`를 현재 운영 상태와 다음 우선순위에 맞게 업데이트했다.
- 런타임 로그가 git 상태에 잡히지 않도록 `.gitignore`에 `backend/logs/`를 추가했다.
- upload 화면에서 chunk 설정을 직접 조절할 수 있도록 `target_length`, `overlap` 입력 UI를 추가했다.
- 기본 chunk 설정값을 `target_length=800`, `overlap=120`으로 두고, 업로드 파이프라인의 chunk/index 단계가 같은 값을 사용하도록 backend/frontend를 연결했다.
- chunk 설정 검증을 추가해 `target_length >= 100`, `overlap >= 0`, `overlap < target_length` 조건을 강제했다.
- 현재 서버와 RAG 서버 양쪽에서 backend 문법 검사와 frontend build를 다시 확인했다.
- RAG 서버에 변경 파일을 다시 반영하고 frontend/backend를 재기동한 뒤 `/upload` 응답을 재확인했다.
- Azure OpenAI embedding provider를 backend에 연결하고 `backend/.env.runtime` 비공개 설정 파일 방식으로 runtime config를 읽도록 정리했다.
- active embedding provider/model별로 Chroma collection을 분리하고, `POST /index/rebuild` 전체 재인덱싱 API를 추가했다.
- RAG 서버 backend를 `azure_openai / text-embedding-3-small`로 재기동했다.
- RAG 서버에서 Azure embedding 기준 전체 재인덱싱을 실행했다.
- RAG 서버 `POST /retrieve`에서 Azure embedding collection 응답을 확인했다.

## 2. 완료된 작업
- RAG 서버 frontend/backend 재기동 완료
- RAG 서버 `3000/8000` 응답 재확인 완료
- port forwarding 운영 기준 정리 완료
- `TODO.md` 현재 상태 반영 완료
- `README.md` 현재 상태 반영 완료
- `docs/status.md` 현재 상태 반영 완료
- `backend/logs/` git ignore 처리 완료
- upload 화면 chunk 설정 입력 UI 추가 완료
- chunk/index 사용자 설정값 연동 완료
- RAG 서버 재배포 및 재기동 완료
- Azure OpenAI embedding 연결 완료
- Azure embedding 전체 재인덱싱 API 추가 완료
- Azure embedding 기준 retrieval 응답 확인 완료

## 3. 검증 내용
- RAG 서버 backend `/health` 응답 `200`
- RAG 서버 frontend `/upload` 응답 `200`
- RAG 서버 frontend `/chat` 응답 `200`
- frontend는 `127.0.0.1:3000`, backend는 `127.0.0.1:8000` 바인딩 기준으로 동작 확인
- 외부 브라우저에서는 SSH port forwarding 후 화면 확인 가능함을 확인
- 현재 서버 backend `py_compile` 통과
- 현재 서버 frontend `npm run build` 통과
- RAG 서버 backend `py_compile` 통과
- RAG 서버 frontend `npm run build` 통과
- RAG 서버 `/health`에서 `embedding_provider=azure_openai`, `embedding_model=text-embedding-3-small` 확인
- RAG 서버 `POST /index/rebuild` 결과:
  - file_count `2`
  - collection `insurance_document_chunks__azure_openai__text-embedding-3-small`
- RAG 서버 `POST /retrieve`에서 Azure embedding collection 기준 hit `3` 확인

## 4. 이슈 및 메모
- 현재 frontend/backend는 외부 공개 바인딩이 아니라 `127.0.0.1` 기준으로 운영 중이다.
- 따라서 화면 확인 시 직접 접속이 아니라 SSH port forwarding이 필요하다.
- parser 옵션은 현재 구현/검증용으로는 유용하지만, 사용자 기본 흐름에서는 자동 정책 고정이 더 적합할 가능성이 크다.
- 실제 retrieval 병목은 여전히 `hash embedding` 품질과 전역 ranking 품질에 가깝다.
- hash embedding 병목 제거 후에는 retrieval 질문 세트 기준 실제 개선 폭을 다시 확인해야 한다.

## 5. 다음 작업 계획
- retrieval 질문 세트 기준으로 Azure embedding 적용 후 결과를 재검증한다.
- embedding 적용 전후 관찰 포인트를 정리하고 retrieval 품질을 다시 기록한다.
- parser 영향과 embedding 영향 범위를 분리해서 다시 비교한다.
- sample PDF 기준으로 `Docling`과 `PyMuPDF` parse 품질 비교는 그 다음 순서로 진행한다.

## 6. 다음 세션 시작 포인트
1. `docs/daily/2026-03-28.md` 확인
2. `docs/status.md` 확인
3. retrieval 질문 세트 기준 Azure embedding 재검증부터 진행
4. embedding 영향과 parser 영향 분리 기준 정리
