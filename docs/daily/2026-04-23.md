# 2026-04-23

## 작업 요약
- RAG 서버 runtime 기준을 다시 확인하고 frontend/backend를 `3000/8000` 기준으로 유지했다.
- 브라우저/IDE port forwarding UI가 `127.0.0.1:3001`을 표시할 수 있지만, 서버 기준 runtime은 frontend `127.0.0.1:3000`, backend `127.0.0.1:8000`만 사용한다.
- `/chat` 상단 `Uploaded Insurance documents` header가 Chat content column과 왼쪽 정렬이 맞도록 CSS를 조정했다.
- `/chat` Search API payload에서 `filters.document_type`을 비활성화했다.
- document type 추론은 rewrite trace/routing hint에는 남기되, Search API recall을 제한하는 filter로는 사용하지 않는다.
- `chunk_types`, `filters.year`도 기존처럼 보내지 않고, payload는 `return_format=json`, `keyword_vector_weight=0.3`, `top_k=30`, `final_k=10` 기준을 유지한다.
- backend 기본 LLM 호출 temperature를 `DEFAULT_CHAT_TEMPERATURE = 0.3`으로 변경했다.

## 검증
- `python3 -m py_compile backend/app/main.py backend/app/query_routing.py` 통과.
- `backend/.venv/bin/python -m unittest discover -s backend/tests -p 'test_*.py'` 통과.
- RAG 서버에 backend/frontend 변경을 반영하고 backend `/health` 응답을 확인했다.
- RAG 서버 backend에서 `DEFAULT_CHAT_TEMPERATURE = 0.3` 반영을 확인했다.

## 질의 분석
- `법인계약건일 경우 자동이체 변경(해지)가 가능해?`는 `010-300200_법인계약 고객센터 상담 기준.docx`와 `011-300227_계약사항 변경 업무기준 요약표.docx` 계열에 답변 근거가 있었다.
- 해당 케이스에서 answer가 비는 원인은 document type filter보다 상품명/보험명 mismatch 필터가 `법인계약건` 같은 업무 키워드를 상품 후보처럼 오판해 answer context를 제거하는 경우가 핵심이었다.
- `미성년자인 계약자가 제지급 요청 시 필요 서류가 뭐야?`의 직접 근거는 `009-300197_제지급_우편.docx-1.md`의 `법정대리인 내방_친권자 또는 미성년후견인` chunk다.
- 위 제지급 케이스에서 chunk `#43`, `#47`은 후견인/사망 청구권자 관련 내용으로 직접 답변 근거가 아니다.
- `신한큐브종합건강상해보험 6년 유지 후 해지 시 해약환급금 기준/산출식/산출근거` 케이스는 판매약관 `제22조 해약환급금`만 반복 검색되어 실제 산출식이나 산출방법서 context가 부족했다.

## 다음 작업
- 상품명/보험명 mismatch 필터에 business/process 문서 예외를 추가하고, 필터 결과가 비면 원본 context로 fallback 하도록 보강한다.
- `009-300197_제지급_우편.docx`의 미성년 계약자/법정대리인 chunk가 상위 context에 안정적으로 올라오도록 rerank/boost를 검토한다.
- 신한큐브 해약환급금 산출식 질의에서 판매약관만 반복되지 않고 산출방법서 계열 문서가 검색되도록 retrieval 보강을 검토한다.
