# Daily Log - 2026-04-01

## 1. 오늘 작업 내용
- 작업 시작 전 `AGENTS.md`, `docs/plan.md`, `TODO.md`, `docs/status.md`, 관련 `docs/*.md`, `docs/daily/2026-03-31.md`를 순서대로 다시 읽고 오늘 우선순위를 정리했다.
- parser 고도화 1순위 작업으로 backend `POST /parse/quality` 경로에 PDF garbled text heuristic을 추가했다.
- 품질 체크 기준을 `Jaccard Similarity`, `Levenshtein Distance` 외에 replacement character, control character ratio, suspicious symbol ratio, parsed/reference text length ratio까지 확장했다.
- PDF 품질 경고를 단일 `파싱 품질주의` 문자열이 아니라 warning reason 목록으로 저장하고 `GET /pipeline/files` 응답에 함께 노출하도록 정리했다.
- frontend `/upload`에서 quality 결과에 PDF garbled text 여부, suspicious symbol ratio, parsed/reference length ratio, replacement/control character count, warning reason 목록을 표시하도록 수정했다.
- `docs/plan.md`, `TODO.md`, `docs/status.md`, `docs/backend.md`, `docs/ui.md`를 오늘 작업 상태에 맞게 갱신했다.
- RAG 서버에서 `Docling`과 `PyMuPDF` 비교를 시도했고, `Docling`은 import 및 converter 초기화는 성공했지만 샘플 PDF 2건 모두 `timeout 30` 안에 변환이 끝나지 않는 것을 확인했다.
- local 기준 `PyMuPDF`와 reference extractor 비교에서는 약관 PDF가 warning 없이 통과했고, 산출방법서 PDF는 `jaccard=1.0`, `length_ratio=1.0`인데도 `suspicious symbol ratio=0.4459`로 heuristic warning이 발생하는 과탐을 확인했다.
- 이후 heuristic을 `reference suspicious ratio delta` 기준으로 보정했고, 같은 산출방법서 PDF에서 warning이 내려가지 않는 것을 다시 확인했다.
- parser catalog 기본값을 `Legacy auto parser`로 바꾸고, PDF는 `PyMuPDF`를 우선 쓰는 운영 정책으로 정리했다.

## 2. 완료된 작업
- PDF garbled text 감지 기준 추가 완료
- upload 품질 경고 세분화 완료
- parser 품질 경고 reason 노출 완료
- parser 품질 비교 전략 문서 반영 완료
- `PyMuPDF` / reference extractor 기준 샘플 PDF 품질 비교 완료
- `Docling` PDF 변환 timeout 30 기준 실측 완료
- 산출방법서 PDF heuristic 과탐 보정 완료
- PDF 기본 parser 정책을 `Legacy auto / PyMuPDF 우선`으로 조정 완료
- 오늘 daily 기록 생성

## 3. 이슈 및 문제
- 현재 PDF garbled text 감지는 heuristic 기반이라 실제 보험 PDF에서 과탐/미탐 가능성이 남아 있다.
- `Docling`은 RAG 서버에서 초기화는 되지만, 샘플 PDF 2건이 30초 안에 끝나지 않아 운영 parser로 쓰기엔 속도 리스크가 있다.
- 현재 서버에서는 UI를 직접 확인하지 못하므로 실제 화면 검증은 RAG 서버 `3000`에서 다시 확인해야 한다.

## 4. 다음 작업 계획
- `Docling` PDF 변환 장시간 실행 원인을 확인하고 PDF 기본 parser 정책을 정리한다.
- 이후 `docs/answer-eval.md` 기준으로 대표 질문 answer/citation 품질 기록을 시작한다.
