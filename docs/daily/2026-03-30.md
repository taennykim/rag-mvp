# Daily Log - 2026-03-30

## 1. 오늘 작업 내용
- 작업 시작 전 `AGENTS.md`, `TODO.md`, `docs/plan.md`, `docs/*.md`, `docs/daily/2026-03-29.md`를 다시 읽고 오늘 우선순위를 정리했다.
- RAG 서버 frontend `3000`, backend `8000`을 다시 점검하고 재기동했다.
- parser availability와 upload list가 live API 기준 정상인지 확인했다.
- backend에 `POST /chat` grounded answer API를 추가했다.
- `POST /chat`은 기존 retrieval 결과를 재사용해 context를 만들고, answer generation은 retrieval context만 사용하도록 제한했다.
- answer generation prompt에 insufficient-context 규칙을 넣고, 응답은 `STATUS` / `ANSWER` 형식으로 파싱하도록 구현했다.
- citation은 모델 출력에 의존하지 않고 retrieval hit metadata에서 별도로 구성하도록 정리했다.
- frontend `/chat`을 retrieval test 중심 화면에서 answer + citation + retrieved chunks를 함께 보는 구조로 수정했다.
- answer panel, citation card 스타일을 `globals.css`에 추가했다.
- local 기준 backend `py_compile`, frontend `npm run build` 검증을 완료했다.
- Azure OpenAI endpoint 설정을 확인한 결과 embedding deployment만 존재하고 chat deployment 이름은 runtime config에 없음을 확인했다.
- Azure endpoint의 deployment 목록 조회를 시도했지만 현재 응답은 `404`였다.
- common deployment 후보를 probe한 결과 `gpt-4o`가 실제 chat completion 응답을 반환하는 것을 확인했다.
- local `backend/.env.runtime`에 `AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-4o`를 추가했다.
- RAG 서버 `backend/.env.runtime`에도 `AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-4o`를 반영하고 backend를 재기동했다.
- `/chat` 실질문 기준 answer generation 응답을 검증했다.
- 질문 `계약자 변경 시 동의가 필요한 대상은 누구인가?`에 대해 grounded answer와 citation 2건 이상이 반환되는 것을 확인했다.
- 질문 `계약자 변경을 위한 서류를 알려줘` 기준으로 retrieval hit와 answer를 비교해, 조건부 서류가 일반 서류처럼 합쳐지는 품질 이슈를 확인했다.
- answer prompt를 수정해 `공통 서류`와 `조건부 추가 서류`를 분리하고, 조건부 항목을 무조건 항목처럼 말하지 않도록 보정했다.
- prompt 수정 후 같은 질문으로 다시 검증해 answer가 공통/조건부 구조로 분리되는 것을 확인했다.
- `/chat` 질문 입력 기본값 `계약자 변경`을 제거하고 빈 상태로 시작하도록 수정했다.
- RAG 서버 frontend `3000`에서 stale `next` 프로세스와 `.next` 캐시 영향으로 반영이 꼬이는 문제를 정리하고 새 빌드가 실제로 서빙되는 것을 확인했다.
- `(무)종신보험표준형_20210101_산출방법서.doc`의 실제 chunk 7개를 조회해 현재 chunk 경계와 표/수식형 문서 한계를 분석했다.

## 2. 완료된 작업
- `/chat` answer flow backend 구현 완료
- `/chat` answer panel / citation UI 구현 완료
- insufficient-context 처리 규칙 구현 완료
- local backend/frontend 기본 검증 완료
- Azure chat deployment `gpt-4o` 확인 완료
- RAG 서버 chat deployment 반영 및 실응답 검증 완료
- answer prompt 품질 보정 완료
- `/chat` question 기본값 제거 완료
- 산출방법서 `.doc` chunk 구조 분석 완료
- 오늘 daily 기록 생성

## 3. 이슈 및 문제
- Azure OpenAI endpoint에서 deployment 목록을 바로 조회하는 방식은 현재 `404`로 확인되어, deployment 이름은 별도 경로로 확인해야 한다.
- frontend `3000`은 기존 stale `next` 프로세스가 남아 있으면 재기동이 꼬일 수 있다.
- retrieval 품질이 answer 품질의 상한을 결정하므로, answer 문제와 retrieval 문제를 분리해서 봐야 한다.
- 산출방법서처럼 표/수식 중심 문서는 현재 `800/120` 규칙 기반 chunking에서 의미 단위가 완전히 보존되지 않는다.
- answer는 grounded flow로 동작하지만 retrieval chunk가 조건을 섞고 있으면 answer도 조건을 넓게 요약할 수 있다.

## 4. 다음 작업 계획
- retrieval 질문 세트 기준으로 answer 품질과 citation 노출 형식을 같이 점검한다.
- retrieval fail 문항은 answer fail과 분리해서 기록한다.
- 산출방법서 문서군에 대해 표/수식 보존형 chunk 전략을 검토한다.

## 5. 내일 다시 시작할 때
- 먼저 `docs/daily/2026-03-30.md`와 `docs/status.md`를 확인한다.
- 그 다음 `docs/llm.md`와 `TODO.md`를 보고 answer 품질 점검 범위를 정한다.
- 첫 작업은 retrieval 질문 세트 기준 answer/citation 품질 기록 또는 산출방법서 chunk 전략 검토로 잡는다.
