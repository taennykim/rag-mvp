# Daily Log - 2026-03-29

## 1. 오늘 작업 내용
- `rag-mvp` 작업 전 프로젝트 md 문서를 다시 확인하고 현재 기준 문서 컨텍스트를 정리했다.
- 실제 작업 문서는 `AGENTS.md`, `README.md`, `TODO.md`, `docs/*.md`, `docs/daily/*` 기준으로 다시 읽었다.
- `docs/plan.md`, `docs/ui.md`를 현재 구현 상태에 맞게 업데이트했다.
- RAG 서버 frontend `3000`, backend `8000` 재기동을 다시 수행하고 응답을 재확인했다.
- upload 화면에서 `Auxiliary parser` 문구를 `Second parser`로 바꿨다.
- 헤더 `Uploaded Insurance documents` 제목 크기를 줄이고 한 줄로 보이도록 수정했다.
- 전역 CSS를 정리해 기존보다 덜 올드한 톤으로 UI를 리프레시했다.
- header, nav, card, form control, button, retrieval card 스타일을 더 현대적인 방향으로 조정했다.
- upload 화면 상단에 uploaded/indexed/chunk/failure 요약 stat strip를 추가했다.
- upload list 카드 hover와 parsing result/quality metrics 시각 구조를 정리했다.
- RAG 서버 frontend에 최신 파일을 반영하는 과정에서 stale `.next` 산출물과 기존 `3000` 프로세스가 남아 있어 반영이 바로 보이지 않는 문제를 확인했다.
- 기존 `3000` 프로세스를 정리하고, 이후 `.next`를 비운 뒤 rebuild + restart 해서 최신 UI가 실제로 서빙되도록 맞췄다.
- `(무)종신보험표준형_20210101_산출방법서.doc`가 2행으로 보이던 이유를 확인했다.
- 현재 구조는 같은 `original_name` 파일을 다시 업로드하면 `uuid__original_name` 형태의 새 `stored_name`으로 별도 행이 누적된다는 점을 확인했다.
- `Last failure: docling -> none` 표시는 parse history log 기준으로 남아 있는 과거 실패 시도라는 점을 확인했다.
- 다시 테스트할 수 있도록 RAG 서버 기준 업로드 파일, parse metadata, chunk metadata, Chroma index를 모두 초기화했다.
- `DELETE /index/files`가 readonly sqlite 오류로 실패하는 점을 확인했고, 실제 원인은 기존 backend PID가 stale 상태로 남아 있던 것이었다.
- stale backend PID를 종료하고 backend를 재기동해 index/files와 pipeline/files가 모두 빈 상태가 되도록 정리했다.
- 오늘 작업 내용을 `README.md`, `docs/status.md`, `TODO.md`, `docs/plan.md`, `docs/ui.md`, `docs/llm.md`에 반영했다.

## 2. 완료된 작업
- md 문서 컨텍스트 재확인 완료
- `docs/plan.md` 현재 상태 반영 완료
- `docs/ui.md` 현재 UI 상태 반영 완료
- RAG 서버 frontend/backend 재기동 완료
- upload 화면 문구 수정 완료
- upload 화면 UI 리프레시 완료
- stale frontend build 캐시 정리 및 재배포 완료
- duplicate upload / parse history 표시 원인 확인 완료
- RAG 서버 테스트 데이터 초기화 완료
- 다음 세션 시작점 문서 반영 완료

## 3. 검증 내용
- RAG 서버 frontend `127.0.0.1:3000/upload` 응답 `200`
- RAG 서버 backend `127.0.0.1:8000/health` 응답 `200`
- stale `.next` 제거 후 새 CSS/HTML 반영 확인
- live HTML에서 header summary 및 upload stat strip 문구가 실제 반영된 것 확인
- RAG 서버 backend `GET /pipeline/files` 결과 `count=0`
- RAG 서버 backend `GET /index/files` 결과 `count=0`
- 현재 backend PID는 재기동 후 새 프로세스로 동작 확인

## 4. 이슈 및 메모
- frontend 반영이 안 보일 때는 stale `3000` 프로세스 또는 `.next` 캐시가 원인일 수 있다.
- 같은 `original_name` 파일을 다시 업로드하면 현재 구조상 새 `stored_name`이 생겨 목록에 별도 행으로 누적된다.
- `Last failure` 표시는 과거 parse 실패 이력을 로그 기준으로 붙여주는 구조라, 현재 성공 상태와 함께 보일 수 있다.
- `DELETE /index/files`는 현재 환경에서 sqlite readonly 오류가 발생할 수 있어, stale backend 프로세스와 Chroma 상태를 함께 확인해야 한다.
- 기본 샘플 파일은 `backend/data/default-files`에 남겨 두고, 테스트 업로드/parse/chunk/index 데이터만 초기화했다.

## 5. 다음 작업 계획
- `/chat` 화면에 answer generation용 LLM을 연결한다.
- retrieval 결과만 근거로 답변을 생성하는 최소 answer flow를 추가한다.
- insufficient-context 처리와 source citation 출력 형식을 정한다.
- answer 연결 후 chat 화면의 answer panel UI를 붙인다.

## 6. 다음 세션 시작 포인트
1. `docs/daily/2026-03-29.md` 확인
2. `docs/status.md` 확인
3. `/chat` 화면에 LLM answer generation 연결부터 진행
4. answer panel과 citation 포맷을 최소 범위로 먼저 붙이기
