# Daily Log - 2026-04-17

## 1. 오늘 작업 내용
- `AGENTS.md` 순서 기준으로 `README.md`, `docs/plan.md`, `TODO.md`, `docs/status.md`, `docs/*.md`, 최신 daily까지 전체 문서를 다시 확인했다.
- 오늘 작업 시작 전 우선순위를 재정리했다.
- RAG 서버(`10.160.98.178:2022`)에 접속해 backend/frontend 프로세스와 포트 listen 상태를 점검했다.
- backend `127.0.0.1:8000/health`, frontend `127.0.0.1:3000/upload`, `127.0.0.1:3000/chat` 응답을 점검했다.
- 문서 연속성 유지를 위해 `docs/plan.md`, `TODO.md`, `docs/status.md`에 오늘 점검 결과를 반영했다.
- `/chat`의 Custom LLM 입력 필드를 `LLM endpoint`, `LLM model name`, `API Key` 기준으로 정리했다.
- Custom Query Rewrite/Answer LLM에 `Temperature`, `Top-K`, `Max Tokens` 입력을 추가하고 backend 요청 파라미터로 연결했다.
- `/chat` Custom 입력에서 `Temperature`, `Top-K`, `Max Tokens`를 세로 배치에서 한 줄 배치로 변경했다.
- 한 줄 배치 후 화면 깨짐 이슈를 보정하기 위해 `chat-inline-grid` 반응형 규칙(모바일 1열)과 입력 폭 정렬을 추가했다.
- Query Rewrite에서 `query-rewrite-spec.md`는 system prompt가 아니라 user prompt에 포함하는 방식으로 다시 원복했다.
- answer generation user prompt가 `docs/answer-generation-spec.md`를 읽어 답변 생성 기준을 반영하도록 변경했다.
- answer 생성 규칙을 별도 문서 `docs/answer-generation-spec.md`로 분리했다.
- backend custom 호출 payload에 `temperature`, `top_k`, `max_tokens`를 반영하고 값 검증(범위 체크) 로직을 추가했다.
- backend `py_compile`, frontend `npm run build`로 변경사항 검증을 완료했다.
- `/chat` Custom 입력 라벨 `Custom model name`을 `LLM model name`으로 변경하고, 프론트 validation/백엔드 에러 문구를 동일 용어로 정리했다.
- RAG 서버 반영 중 잘못된 대상 경로로 파일이 복사된 이슈(루트 `main.py`, `page.tsx`)를 확인하고, 실제 실행 경로(`backend/app/main.py`, `frontend/app/chat/page.tsx`)로 재동기화했다.
- RAG 서버 frontend를 재빌드하고 backend/frontend를 재기동한 뒤 `127.0.0.1:8000/health`, `127.0.0.1:3000/chat` 응답 `200`을 다시 확인했다.

## 2. 완료된 작업
- md 컨텍스트 전체 파악 완료
- RAG 서버 backend/frontend runtime 상태 확인 완료
- backend `8000`, frontend `3000` listen 확인 완료
- backend/ frontend 핵심 endpoint 응답 `200` 확인 완료
- 오늘 점검 결과 문서 반영 완료
- Custom LLM 입력 구조(`LLM endpoint`/`LLM model name`/`API Key`) 및 파라미터 확장 완료
- `Temperature/Top-K/Max Tokens` 한 줄 레이아웃 반영 완료
- 한 줄 레이아웃 CSS 깨짐 보정 및 RAG 서버 재반영 완료
- query rewrite spec user prompt 원복 및 backend 재기동 완료
- answer generation user prompt에 `docs/answer-generation-spec.md` 반영 및 backend 재기동 완료
- 코드 변경 후 backend/frontend 빌드 검증 완료
- `Custom model name` -> `LLM model name` 라벨/문구 변경 완료
- RAG 서버 실행 경로 재동기화(`backend/app`, `frontend/app/chat`) 완료
- RAG 서버 backend/frontend 재기동 및 `8000/3000` 응답 `200` 재확인 완료

## 3. 이슈 및 문제
- 현재 frontend/backend 프로세스는 동작 중이지만, 화면 기능 검증은 실제 브라우저에서 항목별로 추가 확인이 필요하다.
- 우선순위 작업으로 남아 있는 `치조골 이식/수술특약/판결` 케이스 query rewrite 보강은 아직 미진행 상태다.
- OpenAI 표준 endpoint처럼 `model` 필수를 요구하는 커스텀 서버라면 별도 고정 모델 처리 또는 adapter가 추가로 필요할 수 있다.
- RAG 서버 반영 시 경로를 잘못 지정하면 루트에 동일 파일명이 생성되고 실제 실행 코드가 갱신되지 않을 수 있어, 이후 동기화는 대상 경로를 명시적으로 검증해야 한다.

## 4. 다음 작업 계획
- RAG 서버 브라우저에서 `/chat`의 Query Rewrite LLM 선택, `LLM Question`, 응답시간 표시, Evidence/Reference context 노출을 순서대로 점검한다.
- 치조골 이식/수술특약/판결 케이스 기준으로 query rewrite 규칙과 fallback 결과를 검증한다.
- 필요 시 `docs/query-rewrite-spec.md`와 backend rewrite 규칙을 함께 보강한다.
- RAG 서버 브라우저에서 Custom LLM 입력 UI(`LLM endpoint/LLM model name/API Key/Temperature/Top-K/Max Tokens`) 동작과 validation 문구를 확인한다.
- 다음 동기화 작업 전 RAG 서버 대상 파일 경로(`backend/app/main.py`, `frontend/app/chat/page.tsx`)를 먼저 확인한다.
